# Example environment variables for Lambda Cost Reporting System

# AWS Configuration
AWS_DEFAULT_REGION=us-east-1
AWS_PROFILE=default

# Application Configuration
ENVIRONMENT=dev
DYNAMODB_TABLE_NAME=cost-reporting-dev-clients
S3_BUCKET_NAME=cost-reporting-dev-reports
KMS_KEY_ID=alias/cost-reporting-dev-key

# Logging
LOG_LEVEL=INFO

# Local Development (when using LocalStack)
# AWS_ENDPOINT_URL=http://localhost:4566
# LOCALSTACK_ENDPOINT=http://localhost:4566