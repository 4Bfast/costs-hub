# .env

# Chave secreta para segurança do Flask (cookies, etc.)
# Em produção, use um valor longo e aleatório.
SECRET_KEY='super-secret-key-for-dev'

# URL de conexão para o seu banco de dados PostgreSQL local
# Formato: postgresql://[USUARIO]:[SENHA]@[HOST]:[PORTA]/[NOME_DO_BANCO]
DATABASE_URL='postgresql://postgres:admin@localhost:5432/costs_huby_db_mvp'

# Configurações do servidor Flask
FLASK_HOST='0.0.0.0'
FLASK_PORT='5001'
FLASK_DEBUG='True'

# Configurações de CORS
CORS_ORIGINS='http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000'

# Configurações do AWS SES para envio de emails
SES_REGION='us-east-1'
SES_SENDER_EMAIL='noreply@4bfast.com.br'

# Configurações SMTP do AWS SES (credenciais SMTP específicas do SES)
# Para obter: AWS Console > SES > SMTP Settings > Create SMTP Credentials
SMTP_SERVER='email-smtp.us-east-1.amazonaws.com'
SMTP_PORT='587'
SMTP_USERNAME='AKIAQD2D24GGNJXS45GR'  # Suas credenciais SMTP do SES (não são as mesmas do IAM)
SMTP_PASSWORD='BM+0RBbzkg1nQN0K+OYE7ouTKfZXMjQeXyOX/mYnaF/t'  # Sua senha SMTP do SES (não é a secret key do IAM)
SMTP_USE_TLS='True'

# Email remetente
FROM_EMAIL='noreply@4bfast.com.br'
SENDER_EMAIL='noreply@4bfast.com.br'

# URL do frontend para links em emails
FRONTEND_URL='http://localhost:5173'

# ID da conta CostsHub
COSTSHUB_AWS_ACCOUNT_ID=008195334540

# CONFIGURAÇÃO DE CREDENCIAIS AWS (escolha uma opção):
#
# OPÇÃO 1 - IAM Role (RECOMENDADO para produção):
#   - Anexe a policy AmazonSESFullAccess à role da instância EC2/ECS/Lambda
#   - Não precisa configurar credenciais aqui
#   - Boto3 automaticamente usa a role da instância
#
# OPÇÃO 2 - AWS CLI Profile (desenvolvimento local):
#   - Configure: aws configure --profile costshub
#   - Defina: AWS_PROFILE=costshub
#   - Boto3 automaticamente usa o profile
#
# OPÇÃO 3 - Variáveis de ambiente (NÃO RECOMENDADO):
#   - Descomente as linhas abaixo apenas se necessário
#   - AWS_ACCESS_KEY_ID=sua_access_key
#   - AWS_SECRET_ACCESS_KEY=sua_secret_key
#
# OPÇÃO 4 - SMTP (fallback se SES SDK não funcionar):
#   - Configure SMTP_USERNAME e SMTP_PASSWORD acima
#   - Obtenha credenciais em: AWS Console > SES > SMTP Settings
